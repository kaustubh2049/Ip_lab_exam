let let num = 5; // Change this to any number
let factorial = 1;

for (let i = 1; i <= num; i++) {
    factorial *= i; // factorial = factorial * i
}

console.log(`Factorial of ${num} is ${factorial}`);
